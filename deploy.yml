---
- name: Install software on the target machine
  hosts: localhost
  become: yes  # Use sudo privileges if necessary
  tasks:
    - name: Install git
      package:
        name: git
        state: present  # Ensures the package is installed
